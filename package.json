{
  "name": "personal-finance-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo \"(lint) wired in Phase 1\" && exit 0",
    "typecheck": "echo \"(typecheck) wired in Phase 2\" && exit 0",
    "test": "echo \"(test) wired in Phase 8\" && exit 0",
    "test:e2e": "echo \"(e2e) wired in Phase 8\" && exit 0",
    "format": "echo \"(format) wired in Phase 1\" && exit 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.18.3",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
